# This ApplicationSet is managed by the DemoJam DR automation.
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: vm-disaster-recovery
  namespace: openshift-gitops
spec:
  generators:
    - git:
        repoURL: "https://{{ repo_fqdn }}/{{ github_user }}/{{ repo_name }}.git"
        revision: HEAD
        directories:
          - path: vms/base/*
        requeueAfterSeconds: 30
  template:
    metadata:
      name: "dr-vm-{{ '{{' }} path.basename {{ '}}' }}-{{ target_cluster_name }}"
    spec:
      project: default
      source:
        repoURL: "https://{{ repo_fqdn }}/{{ github_user }}/{{ repo_name }}.git"
        targetRevision: HEAD
        path: "vms/overlays/{{ target_cluster_name }}/{{ '{{' }} path.basename {{ '}}' }}"
      destination:
        name: "{{ target_cluster_name }}"
        namespace: "{{ '{{' }} path.basename {{ '}}' }}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
